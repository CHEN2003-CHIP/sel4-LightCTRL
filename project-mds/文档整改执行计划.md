# 文档整改执行计划

本文用于在后续会话中继续执行或复查 LightDemo 文档整改工作。

## 已完成

- 补充 `1.软件需求分析.md` 的 UML 用例图和用例说明表。
- 修订 `2.概要设计.md` 的架构图、顺序图、状态机图说明。
- 修订 `3.详细设计说明书.md` 的通信图说明和模块接口一致性约束。
- 统一 `7/8/9/10/11` 的文档元信息、版本记录和目录。
- 新增 `14.文档质量审查报告.md`。
- 新增 `15.测试计划.md`。
- 新增 `16.图表清单与规范说明.md`。

## 待用户补齐

- 项目成员。
- 指导老师。
- 学校/学院/课程名称。
- 真实板卡 GPIO 验证结果，如后续完成。
- 虚拟机最终运行命令输出截图或日志摘要。
- Mermaid CLI 渲染后的图片，如要求 docx 中必须嵌入图片。

## 虚拟机建议命令

```bash
make test TEST_RUN_ID=final-v2.0
make qemu-test TEST_RUN_ID=final-v2.0 MICROKIT_SDK=../microkit-sdk-2.0.1
make evidence TEST_RUN_ID=final-v2.0
make defense-report TEST_RUN_ID=final-v2.0
make test-vehicle-sweep TEST_RUN_ID=final-v2.0
```

## 后续可继续执行

1. 根据用户提供的成员、老师、课程信息更新所有封面。
2. 安装 Mermaid CLI 后把 Mermaid 图渲染为图片。
3. 重新生成 `project-docs/*.docx`。
4. 若完成真实板卡验证，更新测试计划、测试报告、追踪矩阵和质量审查报告。
